"""Out-of-band tools (non-transformer utilities).

Modules here are invoked directly from the CLI and are not part of the
transform/import pipeline. Examples:

- `nrdb_archive` — pre-decommission NRDB snapshot export.
"""
